# Sputnik
## An AI-based intelligent system for artificial control of plasma-electromagnetic fields for space exploration and protection

To run AI, first download the data using `get_data.py` and use it using by `ai.py`
```bash
pip3 install torch
pip3 install numpy 
python3 get_data.py
python3 ai.py
```
